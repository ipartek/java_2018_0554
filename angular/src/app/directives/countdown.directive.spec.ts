import { CountdownDirective } from './countdown.directive';

describe('CountdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CountdownDirective();
    expect(directive).toBeTruthy();
  });
});
