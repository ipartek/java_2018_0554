import { FiltroCochePipe } from './filtro-coche.pipe';

describe('FiltroCochePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCochePipe();
    expect(pipe).toBeTruthy();
  });
});
